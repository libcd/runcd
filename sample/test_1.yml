# this example yaml file demonstrates the ability to start
# multiple build containers

script:
  backend:
    image: golang:1.5
    commands:
      - go version
  frontend:
    image: node:5.0.0
    commands: node -v
